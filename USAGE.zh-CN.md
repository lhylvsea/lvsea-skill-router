# 中文使用说明

## 触发词与调用方式

“帮我选 Skill”“精准调用 Skill”“我装了很多技能但智能体没调用”“这些 Skill 描述太宽泛”“先路由再执行”。可以显式输入 `$lvsea-skill-router`，也可以在项目规则中要求复杂任务先调用本 Skill。

## 什么时候调用

以下请求应优先显式调用 `$lvsea-skill-router`：

- “我装了很多 Skill，不知道该用哪个”；
- “请先精准选择 Skill，再完成这个项目”；
- “这几个 Skill 描述很像，帮我判断哪个真的适合”；
- “这个任务需要研究、写作、渲染或发布多个阶段，请排好顺序”；
- “不要因为关键词相同就误选宽泛 Skill”。

用户已经明确指定 `$skill-name` 时，不要用路由器替换它。

## 输入

最少需要：

1. 用户任务或一段明确的 session goal；
2. 当前项目路径，默认是当前工作目录；
3. 当前 Agent 能访问的 Skill 根目录，默认由脚本自动发现。

任务越具体，路由越可靠。最好说明最终要交付什么文件或结果、使用什么平台、有哪些硬约束、如何验收。例如“做一个网页”不如“读取 SQLite 设备数据，输出可运行的本地 Web 看板，并检查移动端布局”。

## 主要流程

1. 用 `scripts/route_request.py` 扫描真实 Skill 清单；
2. 读取它输出的 `mcp_rerank`，调用 `jev` MCP 的 `jev_rerank`；
3. 取前 2 至 4 个候选，用 `jev_decide` 检查交付物匹配、边界冲突和是否应当拒绝自动选择；
4. 明确选择一个 Skill、一个短流水线、询问一个澄清问题，或报告没有匹配项；
5. 读取所选 Skill 的 `SKILL.md` 后再执行用户任务；
6. 在结果中说明选了什么、为什么选、哪个近似候选被排除、是否使用 Jev 或 fallback。

## 输出

路由报告至少包含：

- `selected_skills`：名称、绝对路径和执行顺序；
- `why`：与用户交付物直接相关的证据；
- `rejected_near_match`：最容易误选的候选及排除理由；
- `decision_source`：`jev` 或 `fallback`；
- `confidence` 和 top-two margin；
- `missing_dependency`：未安装的工具、MCP 或下游 Skill；
- `next_action`：读取哪个 `SKILL.md`、询问什么，或转交 `find-skills`。

## 应用场景速查

### 场景 1：资料做成 PPT 还是网页

输入：“把这份季度资料做成一页可展示材料。”

预期行为：不要直接选 PPT。先询问最终需要 `.pptx`、HTML 还是图片；用户回答后再加载对应 Skill。

### 场景 2：数据分析与设备管理看板

输入：“从设备数据计算产量、停机率，再输出可运行的工业看板。”

预期行为：将数据分析和前端实现视为两个阶段；先分析数据和指标，再实现看板，最多加载三个有明确职责的 Skill。

### 场景 3：保存失败的本地 Web 系统

输入：“修复保存失败，检查页面请求、后端校验、SQLite 写入和真实记录回归。”

预期行为：优先选择调试/工程 Skill；把前端设计 Skill 作为近似候选排除，除非用户还要求视觉改版。

### 场景 4：发布一个新 Skill

输入：“创建中文 Skill，补四个场景，运行测试并发布到 GitHub。”

预期行为：选择 Skill 创建和发布相关流程；保留机器验收和真实 smoke test，不能只生成一个 `SKILL.md` 就报告完成。

## 故障处理

- `jev` MCP 不可用：保留本地 shortlist，标记 `fallback`，当第一名和第二名接近时暂停并询问。
- 没有候选：报告 `find-skills` 交接和搜索词，不执行安装。
- 候选描述过长或过宽：运行 `grade_skill.py`，把报告作为 Skill 维护建议；不要在路由任务中偷偷修改它。
- 候选路径重复：以项目 > 用户 > 插件为准，并在报告中显示去重结果。
- 任务中途改变：重新生成路由，不沿用上一轮选择。
